;通報
@COM408
TIME += 10

FOR COUNT,1, CHARA_ACTIVE
	IF !TALENT:COUNT:ＥＮＥＭＹ
		;周りから賞賛?フラグ
		LOCAL:2 = 1
		CONTINUE
	ENDIF
	SIF CFLAG:COUNT:現在位置 != CFLAG:MASTER:現在位置
		CONTINUE
	LOCAL=COUNT
	BREAK
NEXT
LOCAL:1=TALENT:LOCAL:ＥＮＥＭＹ
;1/2で逃亡、1/2で逮捕
PRINTFORML 周囲が迷惑そうな顔をしているにも関わらず声を掛け続けている男を通報しよう。
PRINTL 　　　・
PRINTL 　　　・
PRINTW 　　　・

IF RAND:2
	PRINTFORML %CALLNAME:MASTER%が警察に通報しようとしていることを敏感に察知したのか、
	PRINTFORMW %CALLNAME:LOCAL%はそそくさと逃げ出してしまった・・・
	CFLAG:LOCAL:現在位置=場所_ダミー
ELSE
	PRINTFORMW %CALLNAME:LOCAL%は%CALLNAME:MASTER%が呼び出した警察に捕まった
;	CALL DEL_IDOL(LOCAL)
	DELCHARA LOCAL
	CHARA_ACTIVE--
ENDIF
;0530 バグ対策
TARGET=0

LOCAL = 0
FOR COUNT, 1, CHARA_ACTIVE
	IF CFLAG:COUNT:現在位置== CFLAG:MASTER:現在位置
		;口上
		IF LOCAL:2
			LOCAL:2=0
			PRINTFORMW 声を掛けられていた人はほっと胸をなでおろしたようだ
		ENDIF
		LOCAL = RAND:10
		CFLAG:COUNT:(屈服IX+LOCAL:1-人物_訪問者)=MAX(0, CFLAG:COUNT:(屈服IX+LOCAL:1-人物_訪問者)-LOCAL)
		SIF RAND:2
			CFLAG:COUNT:好感度 += LOCAL / 2
	ENDIF
NEXT

RETURN 0
